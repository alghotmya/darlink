# Copy to .env and set after deploying the backend.
# Vite only exposes variables prefixed with VITE_.

VITE_API_BASE_URL=https://your-api-id.execute-api.region.amazonaws.com/prod/

# Optional: same value as backend ADMIN_API_KEY to authenticate Admin API calls
VITE_ADMIN_API_KEY=
